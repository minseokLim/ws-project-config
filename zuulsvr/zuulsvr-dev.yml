zuul:
  prefix: /api

management:
  endpoints:
    web:
      exposure:
        include: "*"
        
hystrix:
  command:
    confsvr:
      execution:
        isolation:
          thread:
            timeoutinmilliseconds: 5000